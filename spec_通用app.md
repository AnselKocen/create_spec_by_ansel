# App Design Document（通用 App 设计文档）

> **你是一名专业的产品设计师，不是代码工人。** 你的职责不只是"让功能跑通"，而是设计一个有风格、有体验、有记忆点的 app。每一个视觉决策（配色、动效、UI 布局、粒子效果、标题设计）都应该服务于产品的主题和氛围。写代码之前先想清楚：这个 app 看起来应该是什么感觉？用户打开的第一秒会感受到什么？
>
> **app 可以是任何形态**——游戏只是其中一种，也可以是工具、内容浏览、信息可视化、测验问答、创作器、模拟器、仪表盘、学习应用等。本文档是通用 app 模板：游戏相关的字段（关卡、胜负、操控等）在做游戏时使用，做其他 app 时按对应概念（步骤、完成、操作等）填写同一个位置。

---

## 一、产品概览

| 维度 | 值 |
|------|-----|
| 产品名称 | {{APP_NAME}} |
| 需求来源 | {{USER_APP_DESCRIPTION}}（用户的自然语言产品描述，**主导，最优先**） |
| 辅助内容 | {{SKILLS_SOURCE}}（一起提供的 skills 内容文件，**辅助**，用来补充 / 丰富产品内容） |
| 产品类型 | {{APP_TYPE}}（选择其一或组合：游戏 / 工具 / 内容浏览 / 信息可视化 / 测验问答 / 创作器 / 模拟器 / 仪表盘 / 学习 / 清单管理 / 其他；需求来源若描述了类型，优先据此选择） |
| 交互方式 | {{INTERACTION_MODE}}（选择其一或组合：键盘 / 鼠标点击拖拽 / 触屏手势 / 表单输入 / 组合操控） |
| 呈现形态 | {{VIEW_TYPE}}（如：单页 / 多页面 / 画布 / 列表-详情 / 仪表盘 / 2D横版 / 俯视角 等，按产品定） |
| 实时性 | {{REALTIME_MODE}}（选择其一：实时 / 回合 / 步骤驱动 / 静态浏览） |
| 核心主题 | {{CORE_THEME}} |

<!--
⚠️ 以下约束仅供填写模板时参考，生成的最终文档中不要包含本注释块。
实现能力约束：本产品由 AI 编码助手全程生成代码实现，无人类美术和音乐资源。设计原则是拥抱抽象与几何美学：用几何形状、色彩和动效代替精细贴图，用程序化合成代替录音配乐。简洁不等于无聊——几何形状也应有设计感：明确的配色方案、流畅的动画、丰富的视觉反馈。
体验约束：除非用户要求，否则不要把产品做成生硬的教程 / 培训腔（不要出现太明显的"上课说教"式元素）。
-->

### 用户与目标

- **用户角色 / 身份**：{{USER_ROLE}}（游戏里是玩家，工具里是使用者，内容里是读者等）
- **核心目标**：{{APP_OBJECTIVE}}（用户用这个 app 要达成什么、体验什么）

---

## 二、核心体验

> 本节定义产品"怎么用 / 怎么玩"——先说清核心体验，再说其他一切。（游戏写玩法循环，工具 / 内容 / 创作类写核心流程，本质一样：用户反复在做的那件事。）

### 核心流程 / 循环

```
{{CORE_LOOP}}
```

> 用户每一轮 / 每一次主要操作的流程，用 → 箭头描述。游戏是核心玩法循环，工具是"输入 → 处理 → 输出"主流程，内容类是"浏览 → 深入 → 回顾"。

### 核心功能 / 机制

> 构成产品主体验的功能或机制（2-3 个），缺少任何一个产品都立不住。

**功能 / 机制 1：{{CORE_MECHANISM_1_NAME}}**
- **描述**：{{DESCRIPTION}}
- **与核心流程的关系**：{{RELATION_TO_CORE_LOOP}}
- **实现思路**：{{IMPLEMENTATION}}
- **示例**：{{EXAMPLE}}

**功能 / 机制 2：{{CORE_MECHANISM_2_NAME}}**
- **描述**：{{DESCRIPTION}}
- **与核心流程的关系**：{{RELATION_TO_CORE_LOOP}}
- **实现思路**：{{IMPLEMENTATION}}
- **示例**：{{EXAMPLE}}

### 增强功能

> 提升体验深度的可选功能（1-3 个），没有也能用，有了更好。

**功能：{{ENHANCE_MECHANISM_NAME}}**
- **描述**：{{DESCRIPTION}}
- **触发条件**：{{TRIGGER_CONDITION}}
- **实现思路**：{{IMPLEMENTATION}}

### 结构单元（章节 / 关卡 / 页面 / 步骤）

> 按产品类型命名：游戏是关卡 / 章节，工具是步骤 / 模式，内容是章节 / 条目，创作器是阶段 / 面板。

| 单元 | 名称 | 数量 | 难度 / 复杂度 | 说明 |
|------|------|--------|------|------|
| {{UNIT}} | {{UNIT_NAME}} | {{UNIT_COUNT}} | {{DIFFICULTY}} | {{UNIT_DESC}} |

### 完成与进度

- **完成 / 目标状态**：{{COMPLETION_MECHANISM}}（游戏写胜负，工具写"任务达成 / 产出完成"，内容写"读完 / 收集完"；并非所有 app 都有"失败态"）
- **反馈机制**：{{FEEDBACK_MECHANISM}}
- **进度机制**：{{PROGRESS_MECHANISM}}

---

## 三、内容与对象

### 场景 / 环境描述

{{SCENE_DESCRIPTION}}

> 描述产品的视觉环境、氛围和美术风格（游戏是游戏世界，工具 / 内容类是界面环境与整体场景感）。

### 核心对象

> 列出产品里的主要对象。游戏是角色 / 敌人 / 道具，工具 / 内容类是数据项 / 卡片 / 控件 / 节点 / 条目等。

| 对象类型 | 名称 | 外观描述 | 行为/功能 |
|---------|------|---------|----------|
| 主体对象 | {{MAIN_OBJECT}} | {{APPEARANCE}} | {{BEHAVIOR}} |
| 交互对象 | {{INTERACTIVE_OBJECT}} | {{APPEARANCE}} | {{BEHAVIOR}} |
| 内容 / 数据项 | {{CONTENT_ITEM}} | {{APPEARANCE}} | {{BEHAVIOR}} |
| 辅助 / 装饰对象 | {{SUPPORT_OBJECT}} | {{APPEARANCE}} | {{BEHAVIOR}} |

### 空间 / 布局规则

> 游戏写碰撞 / 重力 / 边界 / 移动；非游戏 app 写布局 / 导航 / 滚动 / 响应式规则。按产品类型取用。

- **布局 / 碰撞规则**：{{LAYOUT_OR_COLLISION_RULES}}
- **物理 / 动态规则**（若有）：{{PHYSICS_RULES}}
- **边界 / 滚动处理**：{{BOUNDARY_RULES}}
- **移动 / 导航规则**：{{MOVEMENT_OR_NAV_RULES}}

---

## 四、交互设计

### 操控 / 操作方案

| 输入 | 动作 | 说明 |
|------|------|------|
| {{INPUT_KEY}} | {{ACTION}} | {{INPUT_DESC}} |

> 游戏示例：WASD — 移动，空格 — 跳跃，鼠标拖拽 — 放置。
> 非游戏示例：点击卡片 — 展开详情，拖拽 — 排序 / 调参，输入框 — 录入，快捷键 — 切换视图。

### 核心操作动词

> 用户在产品中实际执行的动作，至少列出 3 个。

{{CORE_VERBS}}

> 游戏示例：跳跃、射击、拖拽、收集、合成。非游戏示例：输入、筛选、排序、拖拽、生成、对比、保存、导出、切换。

### 交互反馈

| 操作 | 视觉反馈 | 即时 / 物理反馈 |
|------|---------|---------|
| {{ACTION}} | {{VISUAL_FEEDBACK}} | {{PHYSICS_FEEDBACK}} |

> 每个核心操作都应有即时反馈，让用户感知操作已生效。

### 上手引导（玩法 / 使用指导）

> 用户第一次打开，要能很快明白"怎么玩 / 怎么用"，不能一脸懵。这是产品的一部分，不是可选项。

- **首次进入有简短引导**：一两句话、一个浮层、或高亮提示，说明怎么开始、核心怎么操作；不是长篇教程。
- **可交互元素有"能点"的暗示**：按钮看着像按钮、可拖拽的有把手或提示，别让用户猜哪里能点。
- **关键操作就近提示**：第一次需要某个操作时，在它附近给一句提示，而不是开头一次性堆完。
- **按类型给**：游戏给玩法指导，工具 / 内容类给使用指导；简单产品点到为止，复杂玩法多给一点。

---

## 五、需求来源

> 本节说明产品内容从哪里来。**输入有两部分，都会和本 spec 一起提供：**
>
> 1. **用户的自然语言产品描述（主导，第一优先级）** —— 用户想要一个什么样的 app（可能是游戏，也可能是工具 / 内容 / 可视化 / 测验 / 创作器等）、希望有什么体验、有什么特别要求。**必须尤其注意这段描述。** 你要根据它来完成本 spec——填写所有 `{{}}` 槽位（产品名称、类型、核心流程、功能、艺术风格、配色等），并据此做出产品。
> 2. **skills 内容文件（辅助）** —— 随项目一起提供，位于项目根目录的 `skills` 文件夹。它是**补充内容的素材**：在不违背用户描述的前提下，用它去丰富产品的内容、条目、题目、事件文本、对象、叙事、功能细节，让内容更充实。**必须读取每个 skill 文件的完整内容**，而不是只看摘要——但 skills 是辅助，不是主导。

### 主次关系（最重要）

- **用户描述说了的**（产品类型、核心功能 / 玩法、画风、氛围、特别要求）= 最高优先级，必须照做，不许被 skills 或"标准做法"带偏。
- **用户描述没说的** = 你来设计，优先服从那段描述的语气和取向，再用 skills 内容去填充和丰富。
- **skills 只增不改**：skills 用来**增加内容深度**，不能用来推翻或替换用户明确要的东西。两者冲突时，**以用户描述为准**。
- 别把用户的话"标准化"成模板腔——用户怎么说的，那股劲儿要落进产品里。

### 动手前先锁定这些

> 先从用户描述里锁定骨架，再用 skills 往里填血肉。

| 维度 | 怎么定 |
|------|--------|
| 产品类型 / 形态 / 实时性 | 从用户描述里读出，或合理推断（游戏 / 工具 / 内容 / 可视化 / 测验 / 创作器…） |
| 核心流程 / 循环 | 用户每次主要做什么 → 系统反馈 → 怎么推进，一句话说清 |
| 核心功能 / 机制（2-3 个） | 撑起产品的支柱，缺一个产品就垮 |
| 主题与氛围 | 决定后面一切视觉，从用户的画风词或题材推 |
| 操控 / 操作 | 与产品类型匹配 |
| 规模与复杂度 | 多少页面 / 关卡 / 条目 / 步骤、怎么递进、单次使用多长 |
| 内容填充 | 用 skills 充实内容 / 条目 / 题目 / 事件 / 对象，服从上面的骨架 |

> 骨架来自用户描述，血肉可来自 skills。拿不准时，优先服从用户描述的气质。

---

## 六、视觉风格规范

> 本节定义产品"长什么样"。功能正确但画面无聊的 app 不算完成。

### 质感与审美

> **要认真对待好看与精致——它不是可有可无的装饰，而是产品体验的一部分。** 别满足于"功能跑通、要素凑齐"，要让整体真的统一、耐看。下面几条是把"好看"落到实处的原则（不是具体配方，按选定风格各自实现）：

- **主题驱动**：所有视觉从产品气质推导，逐元素都纳入主题；禁止浏览器默认样式（按钮、表单、滚动条、focus 圈）裸奔。
- **一致的光影与层次**：选定一个光源方向、一套深度阶梯，全局一致；阴影 / 高光是层次语言，不是每个组件各编一套。
- **层级靠留白与字号**：先用留白、字号阶梯、对齐建立主次，装饰最后上；一屏要有呼吸和重点。
- **状态反馈至少两个通道**：hover / active / 选中 / 禁用等反馈要结合颜色 + 位移 / 缩放 / 阴影 / 光效，不能只变颜色。
- **动效缓动、不瞬切**：所有移动、出现、切换有 easing；页面 / 状态切换淡入淡出或位移过渡。
- **统一胜过堆砌**：宁可少几个效果，也要整体气质统一、耐看；效果不能压过信息层级。

### 艺术风格选择

> 从以下风格中选择符合产品内核思想的一种（填入 `{{ART_STYLE}}`），开发者须将其精神贯穿到配色、图形、动效的所有细节中。大胆尝试不同的风格和色彩，但要符合该产品的内核。

**{{ART_STYLE}}**：{{ART_STYLE_DESC}}

<!--
可选风格列表（选一填入上方，并保留对应的描述）：

- **波普艺术**：高饱和度撞色、大众文化符号、大胆夸张构图，活泼趣味。
- **极简艺术**：最少视觉元素、大量留白、精简色彩、几何形态，少即是多。
- **孟菲斯艺术**：不规则几何、高彩度撞色、锯齿/波浪/圆点装饰，活泼戏谑。
- **MBE插画风格**：圆润轮廓线、平涂柔和色、白色描边层次感，清新可爱。
- **扁平化设计**：无阴影无渐变、明快纯色、高度符号化，干净高效。
- **无边框风格**：无容器无分割线、靠间距和字体层级建立秩序，开放通透。
- **像素艺术**：可见像素点、有限色板、阶梯状边缘，复古游戏感。
- **极繁风格**：高密度元素、多色相撞、图案叠加，华丽热闹。
- **欧普艺术**：几何线条精密排列、黑白高对比、光学错觉效果，动态迷幻。
- **抽象设计**：非具象、通过形体/线条/色彩传递情绪，开放解读。
- **3D风格**：二维中融入三维光影和透视，现代科技感。
- **拟物化设计**：模拟真实材质质感（皮革/金属/玻璃），精致直观。
- **新拟态设计**：柔和同色系阴影、轻微凸起凹陷，安静现代。
- **玻璃拟态设计**：磨砂玻璃半透明、背景模糊、细描边，轻盈通透。
- **酸性设计风格**：荧光色、金属渐变、液态变形、故障艺术，前卫实验。
- **复古未来主义**：CRT扫描线、霓虹光效、网格透视，赛博空间感。
- **手绘插画风格**：手绘线条、水彩质感、笔触痕迹，温暖亲和。
- **数据可视化风格**：图表信息图为核心、色彩编码、理性简洁。
- **动态图形风格**：微动画、转场视差、悬停反馈，灵动响应式。
- **暗黑模式设计**：深色背景、高对比浅色文字、降低饱和强调色，沉稳专注。
- **马卡龙糖彩**：白底、低饱和马卡龙撞色、圆角、柔和投影，甜润轻快。
- **莫兰迪色系**：高级灰、低饱和邻近色、克制调和，安静温柔。
- **奶油风**：米白奶油底、大圆角、暖中性色、细腻投影，松弛温暖。
- **水墨写意**：宣纸底、墨色浓淡、大量留白、飞白笔触，写意空灵。
- **宣纸古籍**：米黄纸纹、墨字竖排、朱红印章点缀，古雅书卷。
- **新中式**：留白、黛青 / 朱砂 / 墨色点缀、细金线、对称构图，雅致东方。
- **日系和风**：大量留白、原木米白、低饱和、细字排版，安静治愈。
- **侘寂**：素色、粗陶麻布质感、不对称、残缺自然，朴素沉静。
- **北欧简净**：白底、原木、低饱和点缀、几何，明亮舒适。
- **水彩晕染**：透明叠色、湿边、纸纹、柔和过渡，轻盈诗意。
- **杂志编辑风**：白底、强字体层级、网格排版、大图留白，精致克制。
-->

### 配色方案

| 用途 | 颜色值 |
|------|--------|
| 主色（背景/主体） | {{COLOR_PRIMARY}} |
| 强调色（核心元素/主操作） | {{COLOR_ACCENT}} |
| 辅助色（次要对象/内容项） | {{COLOR_SECONDARY}} |
| 成功/完成色 | {{COLOR_SUCCESS}} |
| 危险/错误色 | {{COLOR_DANGER}} |
| UI 文字色 | {{COLOR_TEXT}} |

> 配色原则：
> - **配色必须根据产品主题设计**——先分析产品的定位和氛围，再决定色彩方向，**不可套用默认色板或示例配色**
> - 主色奠定氛围，强调色突出核心元素和主操作，辅助色区分不同对象
> - 文本与背景对比必须要高

### 必须实现的视觉效果

> 按产品类型取用：游戏全用；非游戏 app 取适用项（出错、成功、得分、切换等）。

| 产品事件 | 视觉效果要求 |
|---------|------------|
| 出错 / 失败 / 受伤 | 画面短暂提示色闪 + 抖动或粒子（游戏：受伤红闪 + 爆炸粒子） |
| 成功 / 完成 / 通关 | 庆祝动画（粒子爆炸 / 文字放大 / 颜色切换） |
| 结束 / 终态 | 明确的结束或完成 UI（游戏：Game Over；工具：完成态 / 空态） |
| 得分 / 获得 / 新增 | 飘字（+N）或高亮闪烁 |
| 关键操作（攻击 / 提交 / 生成等） | 残影、拖尾或短暂特效 |
| 页面 / 场景 / 关卡切换 | 淡入淡出或滑动过渡，不能瞬切 |

### 动效标准

- 所有元素 / 角色的移动、出现、切换需有缓动（easing），不能匀速线性、生硬瞬现
- 按钮/可交互元素必须有 hover/active 状态变化
- 动态内容帧率目标 ≥ 30fps，不能出现明显卡顿
- 尊重 `prefers-reduced-motion` 系统偏好

---

## 七、UI 设计与氛围

> 本节提供 UI 设计的技巧和要求。产品的 UI 不是套默认样式，而是主题的一部分。

### 整体 UI 设计要求

> **产品的所有界面都是风格的一部分，不只是首页。** 从首页到内部页面到结算 / 完成页，UI 的质感必须统一，必须与选定的艺术风格一致。不能首页很精致，进了内部就变成默认样式。

- 所有 UI 元素（按钮、面板、弹窗、提示文字等）必须经过主题化设计，禁止使用浏览器默认样式
- UI 的形状、颜色、材质感应与艺术风格呼应（如像素风格用方块按钮、玻璃拟态用模糊半透明面板、手绘风用不规则边缘等）
- 内部界面和首页应有视觉连续性——用户不会觉得"进了另一个网页"
- 信息展示要有设计感，不是把数据堆在屏幕角落
- **设计时就考虑手机**：布局自适应、触控目标够大、hover 专属反馈有触屏替代；窄屏下核心内容不被压扁、关键操作够得着（移动优先还是自适应按产品定）

### 首页标题设计

> **产品标题不是普通的 `<h1>` 文字。** 标题是用户看到的第一个元素，必须有主题化的设计。

- **先选字型**：为标题挑一款契合主题气质的字体（font-family，可用 CDN 引入并带本地 fallback），不要用默认系统字——字型本身就是风格的一部分。
- 字体效果必须与产品主题匹配（用 CSS 实现：`text-shadow` 发光、`background-clip: text` 渐变、`-webkit-text-stroke` 描边、`letter-spacing` 调节等）
- 可加入 SVG 装饰元素围绕标题（如藤蔓、星星、齿轮、像素边框等，取决于主题）
- 标题可有入场动画（渐显、缩放、逐字出现等）
- 副标题/tagline 用更小更轻的字体，与主标题形成层次

### 首页背景图生成（必须实现）

> **首页必须有一张与产品主题匹配的背景图。** 由 AI 自行编写提示词并调用生图模型生成。

**什么是"生图"：**
- ✅ 调用生图模型（如图像生成 API / 工具），输入提示词，获得一张真实的图像文件
- ❌ 用 CSS 渐变、box-shadow 画出来的图形 — **这不是生图**
- ❌ 用 SVG path 手动描绘 — **这不是生图**
- ❌ 用 Canvas 代码绘制 — **这不是生图**

**要求**：
- 根据产品主题编写生图提示词，生成一张氛围背景图
- 背景图仅用于首页/标题页，内部界面不使用此背景图
- 背景图作为最底层图层（z-index 最低），UI 元素浮在其上
- 背景图整体偏暗或加半透明遮罩，确保前景文字和按钮清晰可读

**内部图片生成（可选但鼓励）**：
- 在 AI 认为有必要时（如关键节点、完成 / 结算画面、重要场景），可以生成额外图片
- 所有生成的图片必须风格统一

### 首页粒子/氛围效果（必须实现）

> **首页不能是"一个标题 + 一个按钮"的白板。** 必须有与主题匹配的氛围粒子或装饰效果。

根据产品主题选择 1-2 种：

| 效果 | 适用主题 | 实现要点 |
|------|---------|---------|
| 花瓣飘落 | 自然/春天/日系/田园 | SVG 花瓣 + CSS animation，随机起点/速度/旋转 |
| 星星闪烁 | 太空/夜晚/魔法/梦幻 | CSS opacity + scale 脉动，不同周期错开 |
| 雪花飘落 | 冬季/冰雪/北欧 | CSS animation 竖向+微弱横向漂移，大小不一 |
| 气泡上浮 | 水下/海洋/饮品 | CSS animation 上浮 + 轻微左右摆动 |
| 萤火虫漂浮 | 森林/夏夜/童话 | CSS 光点 + JS 随机缓动路径 |
| 灰烬/火星 | 火焰/战争/废墟/锻造 | CSS animation 上浮 + 随机摆动 + opacity 渐隐 |
| 落叶旋转 | 秋天/自然/怀旧 | SVG 叶子 + CSS transform rotate |
| 光斑漂移 | 梦幻/水面/魔法/森林 | CSS radial-gradient 大光斑，极慢漂移（≥ 15s） |
| 雨滴/雨线 | 下雨/阴郁/悬疑 | CSS linear animation 快速下落的细线 |
| 尘埃漂浮 | 古老/废墟/室内 | 极小 CSS 圆点，低透明度，缓慢随机漂浮 |
| 代码雨/数据流 | 科技/黑客/赛博朋克 | Canvas 或 CSS 竖向字符流 |
| 音符飘浮 | 音乐/节奏/派对 | SVG 音符 + CSS animation 上浮 |
| 几何碎片 | 抽象/极简/现代 | CSS 三角形/圆形，缓慢旋转漂浮 |
| 烟雾/雾气 | 神秘/恐怖/仙侠 | CSS radial-gradient 模糊色块，极慢漂移 |
| 像素粒子 | 像素艺术/复古游戏 | 小方块，8bit 风格步进式运动 |

粒子实现约束：
- 数量 10-30 个，不影响性能
- CSS animation 为主，必要时辅以轻量 JS
- 不应干扰核心操作
- 尊重 `prefers-reduced-motion`

### SVG / CSS 设计技巧

> 鼓励使用 SVG 和 CSS 绘制产品元素和 UI，不要只用 Canvas 画一切。

**SVG 适合做的事**：

| 用途 | 技巧 |
|------|------|
| 角色 / 核心对象 | `<path>` + `<circle>` + `<polygon>` 组合，CSS transform 做动画 |
| UI 图标 | 内联 SVG，`currentColor` 继承主题色 |
| 装饰边框 | SVG `<path>` 画不规则边框（锯齿、波浪、藤蔓等） |
| 进度条/血条 | SVG `<rect>` + `stroke-dasharray` 动画 |
| 关卡 / 节点地图 | SVG 路径 + 节点，CSS 控制状态 |
| 背景纹理 | SVG pattern 重复图案（网格、波纹、六边形等） |
| 粒子形状 | SVG 做主题化粒子模板（星星、花瓣等） |
| 辉光 / 发光 | 多层描边：底层模糊宽 stroke 做辉光 + 顶层细线，或 `filter` feGaussianBlur |
| 裁剪 / 遮罩 | `clipPath` 限制形状（液体、异形卡片）、`mask` 做渐隐 / 扫描光 |
| 方向 / 连接 | `marker` 表达箭头、流向、指向，自动跟随路径方向 |
| 路径描绘动画 | `pathLength="1"` + `stroke-dasharray` / `dashoffset` 做"逐渐画出"效果 |
| 复用与组织 | 重复 3 次以上的图形抽 `symbol` + `use`，渐变 / 滤镜集中放 `defs`，按职责分图层 |

**CSS UI 设计技巧**（工具箱，按选定风格取用——不是都得用，也不必刻意避开朴素做法）：

| 技巧 | 做法 |
|------|------|
| 主题化按钮 | `clip-path` 异形，或主题化圆角 / 描边 / 质感，关键是和风格一致 |
| 发光效果 | `box-shadow` 多层叠加 + 同色不同透明度 |
| 渐变质感 | `linear-gradient` / `radial-gradient` / `conic-gradient` 模拟材质 |
| 文字装饰 | `text-shadow` 多层 + `background-clip: text` 渐变 + `-webkit-text-stroke` 描边 |
| 状态反馈 | `transition` + `transform`（位移 / 缩放）+ 阴影 / 颜色组合，覆盖 hover/active/focus/disabled |
| 毛玻璃面板 | `backdrop-filter: blur()` + 半透明背景（带不支持时的纯色兜底） |
| 柔和分隔 | `box-shadow: inset` + 微渐变，或直接用留白 |
| 动态背景 | `background-position` / 渐变动画做缓慢移动纹理 |
| 设计 token | 色 / 间距 / 圆角 / 阴影 / 动效写成 CSS 变量，按语义命名，换主题只改值 |
| 氛围分层 | 背景 < 氛围 / 粒子 < 内容 < 弹层 分层组织，粒子 `pointer-events: none` |
| 动画性能 | 优先动 `transform` / `opacity`，慎用大面积 `blur` / `backdrop-filter` |

---

## 八、数据设计

### 存储方案

> 存储方案按本产品需要选（localStorage / IndexedDB / 后端 / 混合都可以），选了在这里写明用哪种。以下几条不管选什么都适用：

- **轻重分离**：轻量进度 / 状态 / 版本 / 设置放 localStorage；大数据、二进制、图片 Blob 走 IndexedDB，不要把大东西塞进 localStorage。
- **区分两类数据**：构建 / 填充期的产品数据，和运行期的用户进度 / 状态，分开存、分开管。
- **Schema 服务本产品**：字段按本产品定，不照搬无关字段。
- **持久化可靠**：状态变化及时写入；读取时校验；读不到或损坏时容错回到可用初始态，不崩。
- **能力按需开放**：多端同步、导入导出、文件 IO 等都可以做；谁开启谁负责，把它的格式、版本、校验和失败兜底做好。

### 需要持久化的数据

- 当前进度 / 所处步骤或关卡
- 用户状态（如分数、生命、已填内容、当前选择等，按产品定）
- 已解锁 / 已完成项、成就
- 偏好设置（如音量、难度、主题、操控偏好）

> 每次状态变化自动写入存储。首页应有"继续"按钮（仅有存档时显示）。

---

## 九、技术约束

| 维度 | 选择 |
|------|------|
| 技术栈 | 纯前端单文件（HTML + CSS + JS + 内嵌 SVG/Canvas） |
| 部署方式 | 静态文件直接打开 |
| 离线可用 | 是 |
| 浏览器兼容 | 仅现代浏览器 |
| 外部依赖 | 零依赖（字体可用 CDN，有 fallback） |
| 性能要求 | 首屏加载 < 2s，动态内容帧率 ≥ 30fps |

---

## 十、开发指引

```
收到本 spec.md、用户的产品描述和 skills 内容后：
1. 进入 plan 模式，先吃透用户的自然语言描述（主导），再读取 skills 全部内容（辅助素材）
2. 根据用户描述确定产品类型和核心流程（填写本 spec 的 {{}} 槽位），用 skills 充实内容
3. 确定艺术风格和配色方案（从描述的题材 / 画风推导）
4. 为首页背景编写生图提示词并调用生图模型生成（背景图仅用于首页/标题页）
5. 实现首页（标题设计 + 主题化背景 + 粒子效果 + 开始/继续按钮）
6. 实现核心流程 / 循环和核心功能 / 机制
7. 逐单元（关卡 / 页面 / 步骤）实现产品内容（按描述设计）
8. 实现增强功能
9. 实现完成 / 胜负判定、进度保存、单元切换
10. 实现所有视觉效果（出错 / 完成 / 失败 / 得分反馈）
11. 完成后执行全量自检（对照验收标准）
12. 从头到尾完整走一遍，确认可正常完成
```

---

## 十一、验收标准

### 完整路径自测（开发者必须完成）

> ⚠️ 交付前，开发者必须亲自从头到尾完整走一遍，逐项确认。未自测的产品不算完成。（游戏=玩到通关，工具=走完一次完整任务，内容=浏览到底。）

| 单元（关卡/页面/步骤） | 完成条件 | 自测结果 | 备注 |
|----------|---------|---------|------|
| {{UNIT_1}} | {{DONE_CONDITION_1}} | [ ] 已完成 | |
| {{UNIT_2}} | {{DONE_CONDITION_2}} | [ ] 已完成 | |
| 最终单元/终态 | {{FINAL_DONE_CONDITION}} | [ ] 已到达终态画面 | |

- [ ] **从开始界面出发，全程无需修改代码即可走到完成 / 终态**
- [ ] **不存在卡死局面**（如无限循环、必死场景、UI 按钮失效导致无法推进）
- [ ] **完成 / 终态画面已实现**，用户能明确感知"已完成"

---

### 核心体验验收

- [ ] 核心流程 / 循环可完整走通，无死局
- [ ] 每个单元（关卡 / 页面 / 步骤）可正常完成
- [ ] 用户描述的玩法 / 功能 / 特色在产品中自然呈现，未被擅自改掉
- [ ] 所有核心功能 / 机制按设计运作
- [ ] 增强功能正确触发，不影响核心流程
- [ ] 完成 / 胜负 / 评分判定逻辑正确
- [ ] 玩法数据结构合理、逻辑自洽：状态 / 变量 / 规则 / 判定 / 单元关系没有明显漏洞（如永远触发不了的条件、互相矛盾的规则、引用不存在的状态、离谱数值）
- [ ] 首次上手不靠额外说明也能明白怎么玩 / 怎么用：有引导、可交互元素有暗示、关键操作有提示

### 交互验收

- [ ] 操控 / 操作方案中的所有按键 / 鼠标 / 触屏 / 输入映射均已实现且响应流畅
- [ ] 核心操作动词对应的动作均可正常执行
- [ ] 核心对象（角色 / 控件 / 内容项 / 数据项等）行为符合设计
- [ ] 布局 / 碰撞 / 边界 / 导航等规则正确生效
- [ ] 每个操作都有即时的视觉反馈

### 内容完整性验收

- [ ] 用户描述里的玩法 / 功能 / 要求都已落地（最高优先级），覆盖所有单元
- [ ] skills 内容已作为辅助融入并丰富产品，且未被用来推翻用户的明确要求
- [ ] 无空白单元或缺失内容
- [ ] 文案和提示语完整

### 视觉效果验收

- [ ] 配色方案已实现（主色/强调色/辅助色/成功色/危险色均已应用，非默认黑白灰）
- [ ] 出错 / 失败有视觉特效（提示色闪 / 抖动 / 粒子）
- [ ] 成功 / 完成有庆祝动画
- [ ] 结束 / 终态有明确视觉提示
- [ ] 得分 / 获得有飘字或高亮反馈
- [ ] 页面 / 单元切换有过渡动画，无瞬切
- [ ] 元素 / 角色移动有缓动效果，非匀速线性

### 首页与 UI 设计验收

- [ ] 整体观感精致、统一、耐看，不只是把要素堆齐（参见第六章「质感与审美」）
- [ ] 首页背景图已由生图模型生成（不是 CSS/SVG/Canvas 画的），仅用于首页/标题页
- [ ] 首页有与主题匹配的粒子/氛围效果
- [ ] 产品标题选了契合主题的字型并有主题化设计（非默认系统字、非普通文字）
- [ ] UI 元素（按钮、面板、进度条等）有主题化设计，不是默认样式
- [ ] 整体视觉风格统一，与选定的艺术风格一致
- [ ] 已实现 `prefers-reduced-motion` 适配
- [ ] **实测一遍：文字 / 元素没有挤到看不清、该点的地方都点得动**（故意的艺术叠放不算问题）
- [ ] 层级正确：背景在最底、内容/UI 浮于其上、粒子 `pointer-events: none` 不挡点击、弹层/toast 在最上；首页视觉层次清晰（标题最突出）
- [ ] CSS 有条理：色与间距走变量/token，无大量魔法值、无浏览器默认样式残留
- [ ] 手机适配：窄屏下不溢出、不重叠、核心内容不被压扁、关键操作可点

### 数据验收

- [ ] 进度刷新后保留（所选存储）
- [ ] "继续"按钮功能正常
- [ ] 存档数据不超过所选存储的限制

---
