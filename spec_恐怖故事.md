# 恐怖故事游戏设计文档

---

## 一、产品概览

| 维度 | 值 |
|------|-----|
| 产品名称 | {{GAME_NAME}} |
| 来源素材 | {{BOOK_NAME}} |
| 产品类型 | 第一人称恐怖叙事 |
| 章节数 | {{CHAPTER_COUNT}}（6-8） |
| 选择节点数 | {{CHOICE_COUNT}}（每章 2-3 个） |
| 解密小游戏 | 每章末尾 1 个（类型各不同） |
| 结局数 | 1（固定结局） |
| 交互方式 | 鼠标点击（阅读推进 + 选择 + 解密交互） |
| 核心主题 | {{CORE_THEME}} |
| 目标体验 | {{CORE_FEELING}} |
| 生图时机 | first-run-cache |
| 图片存储 | IndexedDB Blob；localStorage 只保存轻量进度 |

<!--
⚠️ 以下约束仅供填写模板时参考，生成的最终文档中不要包含本注释块。
实现能力约束：本工具由 AI 编码助手（Claude Code）全程生成代码实现，输出为单页 HTML 产品，内嵌 CSS + JS，零外部依赖。运行时进度存储于 localStorage，真实图片 Blob 和图片缓存 metadata 存储于 IndexedDB。
-->

### ⚠️ 核心设计理念

> **这是一个用第一人称叙事制造恐惧的游戏，不是配了恐怖背景音的电子书。**
>
> 恐怖感来自三个层面的配合：
> 1. **文字**——故事本身，第一人称的沉浸感，让玩家觉得"这事发生在我身上"
> 2. **视觉**——UI 的阴森质感 + 关键场景的 AI 生成 CG 插图，让玩家"看见"恐怖
> 3. **机制**——选择暗中影响命运，玩家在不知情中走向不同结局，事后回想才觉得后怕
>
> 去掉任何一层，恐怖感就打折。特别是视觉——**没有 CG 插图的恐怖游戏不完整**。

### 恐怖子类型选择

> **开发者必须根据素材内容，从以下子类型中选择 1-2 个作为本游戏的恐怖基调。** 不可笼统地写"恐怖"，必须明确是哪种恐怖。

| 子类型 | 核心恐惧 | 特征与常见手法 |
|--------|---------|--------------|
| 心理恐怖 | 怀疑自己的认知/记忆/理智 | 叙述者不可靠，现实在动摇；记忆与证据矛盾；同一场景每次重访细节不同；角色指出"你"没做过的事但有证据；日记/录音与记忆冲突；分不清幻觉还是现实；结局揭示主角一直在被自己欺骗 |
| 宇宙恐怖 | 人类的渺小和无意义 | 无法理解的存在，知道越多越恐惧；研究越深精神越崩溃；看到不该看的东西后世界观碎裂；古老文献中的知识让人发疯；试图理解的人全部消失或变了；真相不是"有怪物"而是"人类根本不重要"；恐惧来自认知而非威胁 |
| 民俗恐怖 | 古老规矩/仪式/禁忌 | 不能打破的规则，打破了就回不去；村庄/社区有外人不知道的"规矩"；规矩看似荒谬但所有人严格遵守；违反规矩的人以诡异方式消失；仪式细节背后藏着真正恐怖的原因；"我们一直这样做"是最可怕的解释；主角发现自己也是仪式的一部分 |
| 身体恐怖 | 自己的身体在变化/不受控制 | 变异、寄生、不可逆的改变；身上出现不该有的东西（多余的痕迹/组织/感知）；照镜子发现细微但确定的变化；身体做了"你"没有下达指令的动作；吃东西的口味/需求开始异常；变化过程缓慢到你无法确定起点；到接受的那一刻才是最恐怖的 |
| 日常侵蚀 | 熟悉的事物逐渐变得不对 | 从正常开始，一点点偏移，到回过神来已经太晚；家具位置微妙变化但没人动过；日常对话中出现不该知道的信息；时间出现无法解释的空白；每天通勤路上某个细节在变化但说不清是什么；周围的人对异常毫无反应；回头看第一章才发现当时已经不对了 |
| 社会恐怖 | 集体中隐藏的恶意/荒谬规则 | 所有人都觉得正常，只有你觉得不对；集体笑着做可怕的事；质疑规则的人被温和地"纠正"；表面友善实则不可退出的社群；大家用日常语气讨论荒谬的事；你开始怀疑是不是自己才是错的；最终发现你也在不知不觉中参与了 |
| 谋杀悬疑 | 凶手就在身边/真相比凶案更可怕 | 一桩命案拉开序幕，线索指向身边的人；每个人都有不在场证明但都有破绽；调查越深发现死者并非无辜；凶手的动机让人脊背发凉；"你"可能不只是调查者而是参与者；真相揭开后才发现还有更深的一层；最恐怖的不是谁杀了人，而是为什么 |
| 困境恐怖 | 逃不出去/封闭空间中的绝望 | 被困在某个地方（建筑/小镇/时间循环）无法离开；出口存在但总是到不了；空间本身在变化或缩小；被困的不只是"你"一个人，同伴逐渐失踪或变化；尝试逃离反而更深陷其中；最终发现被困的原因与自身有关；比起外部威胁，无法逃离本身就是恐怖 |
| 科技恐怖 | 技术失控/数字世界的入侵 | 手机里出现没拍过的照片；AI 开始说不该知道的事；监控录像里有你不记得做过的行为；算法推送越来越精准地戳中恐惧；程序/系统按照某种"意志"在运行；线上与线下的边界在消融；科技的便利背后藏着某种不可逆的交换 |
| 孩童恐怖 | 童年记忆中的阴影/童真的扭曲 | 孩子说出不该知道的事；童谣/儿歌暗藏恐怖含义；玩伴不是真实存在的；童年记忆被发现与事实严重不符；天真的行为背后是可怕的模仿；游戏规则越来越像仪式；"你"小时候做过一件被遗忘但极其可怕的事 |
| 替身/分身恐怖 | 另一个"你"的存在 | 有人看到"你"出现在你没去过的地方；镜中的自己动作慢了半拍；照片里的你表情不对；身边的人说"你昨天不是说过了吗"但你没有；日记里出现不是你写的字迹；另一个"你"在逐渐取代你的生活；分不清哪个才是真的自己 |
| 诅咒/怨念恐怖 | 不可解除的因果报应 | 某个行为触发了不可逆的后果；警告来得太晚，诅咒已经生效；受害者的怨念以某种形式延续；每个试图帮忙的人都被波及；诅咒有规律但无法打破；追溯源头发现起因是一件看似微不足道的事；最恐怖的是"你"可能就是起因 |
| 寄生/操控恐怖 | 意志被夺取/思想不是自己的 | 脑中出现不属于自己的想法；越来越想做一件不合理的事但停不下来；行为模式在向某个人靠拢；"你"开始说出不像自己会说的话；身边的人先后出现同样的变化；抵抗的意志在减弱而且你对此感到安心；寄生者不是要杀你，是要成为你 |

选定后填入：**本游戏恐怖子类型**：{{HORROR_SUBTYPE}}

### 反套路约束（强制）
**禁止的元素**：
- ❌ "原来是一场梦"结局

**强制的叙事要求**：
- ✅ 恐怖感必须来自**理解**而非**惊吓**——玩家越想越怕，不是被吓一跳
- ✅ 故事开头必须看起来正常，恐怖是一点点渗透进来的
- ✅ 恐怖源必须与素材内容有内在联系，不是给知识套一个恐怖壳
- ✅ 至少有一个"回头看才发现不对"的设计——前面看似正常的细节，后来才意识到恐怖
- ✅ 第一人称视角，玩家 = 主角，全程用"你"
- ✅ 子类型的特征与手法是**参考方向**，必须结合传入的素材内容进行剧情设计，不可直接套用特征列表中的例子作为故事主线

### 章节划分

| 章 | 标题 | 氛围梯度 | 选择节点数 | 关键事件 |
|----|------|---------|-----------|---------|
| {{CH_NUM}} | {{CH_TITLE}} | {{ATMOSPHERE}}（正常/微妙不安/明显异常/恐怖高潮/结局） | {{CHOICE_COUNT}} | {{EVENT}} |

> 章节按恐怖感的递进排列：第 1 章几乎正常，每章加深一层不安，最后 1-2 章是高潮和结局。

### 涉及的 SKILL 清单

| SKILL 路径 | 覆盖的内容模块 |
|-----------|---------------|
| {{SKILL_PATH}} | {{MODULE}} |

> **重要：完整的 SKILL 数据文件已随项目一起提供，位于项目根目录的 skills 文件夹中。构建游戏时，必须读取每个 SKILL 文件的完整内容，将其中的信息融入游戏数据中，而非仅依赖索引摘要自行编造。**

---

## 二、目标人群

### 核心用户

| 维度 | 描述 |
|------|------|
| 画像 | {{PRIMARY_USER}} |
| 使用频率 | {{USE_FREQUENCY}} |
| 设备偏好 | {{DEVICE}} |
| 恐怖耐受度 | {{HORROR_TOLERANCE}}（轻度惊悚 / 中度恐怖 / 硬核恐怖） |

---

## 三、产品目的

### 核心价值

{{CORE_VALUE}}

> 用一句话说清：这个游戏让玩家通过 ____，从而 ____。
>
> 核心价值必须体现「恐怖」和「叙事」双重要素。不是"学知识"，是"在恐怖的沉浸体验中接触知识"。

### 成功标准

{{SUCCESS_CRITERIA}}

> 玩家完成后，什么状态说明设计成功？（以"想重玩看其他结局"和"事后回想仍觉得不安"衡量）

---

## 四、视觉风格规范

> 恐怖游戏的视觉是恐怖感的放大器。UI 本身就应该让人感到不安。

### 整体气质

**阴森诡异**：打开页面就应该感到压抑和不安。色调阴暗、文字像是在发抖、界面有衰败和侵蚀的质感。不是"暗黑酷炫"，是"这个页面本身就让人不舒服"。

### 背景系统

> 背景图必须作为 `IMAGE_ASSET_MANIFEST` 中的 required 资产纳入 `first-run-cache` 流程。最终 app 首次打开时先查 IndexedDB；cache hit 读取 Blob 并解码，cache miss 才调用真实生图工具/API 生成并写入 IndexedDB。

**背景生成要求**：
- 根据故事的核心场景，编写背景图的生图提示词
- 背景图的 `asset_id` 建议为 `title_background`
- 仅当 IndexedDB 中没有对应 `cache_key` 的可用 Blob，或缓存损坏 / prompt 版本变化时，才调用生图模型生成一张阴暗压抑的氛围背景图
- 背景图仅用于标题页（首页），其他页面不使用
- 背景图作为最底层图层（z-index 最低），整体偏暗，所有 UI 元素（标题、按钮等）浮在其上方，确保不被遮挡

**背景实现要求**：
- `background-image` 固定铺满视口，`background-size: cover; background-position: center; background-attachment: fixed`
- 背景图上方叠加一层半透明暗色遮罩（偏黑红或偏黑绿，取决于恐怖子类型）
- **可选增强**：遮罩颜色可随章节变化——前期偏灰，后期偏红/偏暗

### UI 元素风格

> UI 是恐怖氛围的一部分，不是中性容器。

**文字区域**：
- 暗色底 + 半透明，边缘有不规则的"侵蚀"感（可用 CSS clip-path 或不规则 border）
- 文字颜色偏灰白，不要纯白——纯白太干净，不够阴森
- 关键恐怖文字可用特殊样式：颜色渐变为暗红、轻微抖动动画、或者字间距异常

**选择按钮**：
- 暗色底 + 极细边框，hover 时边框变红/变亮 + 轻微抖动
- 选择后有短暂的"墨水扩散"或"裂痕蔓延"效果
- 按钮文字可以在 hover 时轻微变形（letter-spacing 微调）

**章节过渡**：
- 画面渐暗 → 黑屏停顿 1-2s → 新章标题缓慢显现（带轻微抖动或故障效果）
- 可加入"重影"效果：标题文字有一个偏移的半透明副本
- 不要华丽，要压抑

**恐怖 UI 特效（CSS 实现）**：
- **重影/故障效果**：文字或图像的 RGB 通道偏移（用多层 text-shadow 或 CSS filter）
- **滴血/渗透效果**：CSS 渐变从顶部或边缘向下渗透的暗红色，`background: linear-gradient(to bottom, rgba(139,0,0,0.3), transparent)`
- **呼吸/脉动**：界面元素有极缓慢的明暗脉动（opacity 在 0.85-1.0 之间波动，周期 ≥ 8s）
- **抖动/不稳定**：关键恐怖时刻，文字或界面有微弱的 CSS transform 抖动
- **衰败质感**：面板边缘不规则，有"腐蚀"感的 border 或 clip-path

**恐怖警告弹窗（固定功能，必须实现）**：
- 点击"开始游戏"后，**必须先弹出恐怖警告弹窗**，确认后才进入章节主页
- 弹窗内容：警告文字（如"本游戏包含恐怖内容，可能引起不适。确定继续？"）
- 弹窗 UI 必须本身就很恐怖——不是普通的 confirm 框：
  - 背景极暗，弹窗边缘有不规则腐蚀/裂痕效果
  - 警告文字用恐怖风格（text-shadow + transform 模拟扭曲效果）
  - 文字有轻微抖动或故障闪烁动画
  - "继续"按钮用暗红色，hover 时渗血效果加重
  - "离开"按钮用灰色，点击后回到标题页
- 这个弹窗本身就是恐怖体验的开始

### CG 插图风格

> **CG 插图是恐怖视觉的核心。** 风格必须统一，氛围必须到位。

**CG 风格选择（强制，必须从以下 14 种中选 1 种）**：

> **开发者必须根据素材内容和恐怖子类型，从以下 14 种 CG 风格中选择 1 种作为本游戏的统一画风。** 选定后，该风格的英文提示词作为所有 CG 的风格基底，每张 CG 只追加场景描述。不可自行编造风格，不可跨风格混搭。

| 编号 | 风格名称 | 提示词基底 | 适合的恐怖子类型 |
|------|---------|-----------|----------------|
| 1 | 暗色写实插画风 | `dark realistic illustration, ultra detailed, Rembrandt lighting, deep shadow, muted dark tones, cinematic atmosphere` | 通用、心理恐怖、谋杀悬疑 |
| 2 | 水墨恐怖风 | `dark ink wash horror, traditional Chinese ink painting, black ink splatter, wet brush strokes, rice paper texture, minimal color` | 民俗恐怖、诅咒/怨念恐怖 |
| 3 | 复古恐怖版画风 | `vintage horror engraving, woodcut print style, cross-hatching, aged paper, black and sepia tones, Gothic illustration` | 民俗恐怖、宇宙恐怖 |
| 4 | 模糊/失焦的不安感 | `blurred unsettling photography, out of focus, grainy film texture, liminal space, uncanny valley, muted desaturated colors` | 日常侵蚀、替身/分身恐怖 |
| 5 | 铅笔素描/炭笔风 | `dark charcoal sketch, rough pencil horror, unfinished drawing, smudged graphite, torn paper edges, frantic hand-drawn style` | 心理恐怖、困境恐怖 |
| 6 | 老照片/褪色胶片风 | `faded vintage photograph, sepia horror, film grain, heavy vignette, yellowed aged photo, daguerreotype style, ghostly exposure` | 日常侵蚀、诅咒/怨念恐怖 |
| 7 | 浮世绘恐怖风 | `Japanese horror ukiyo-e, supernatural woodblock print, bold outlines, flat composition, unsettling color palette, yokai style` | 民俗恐怖、孩童恐怖 |
| 8 | 肉体/生物恐怖风 | `body horror illustration, organic grotesque, wet slimy texture, irregular biological forms, sickly pale color palette, visceral detail` | 身体恐怖、寄生/操控恐怖 |
| 9 | 儿童绘本扭曲风 | `corrupted children's book illustration, cute style with wrong details, pastel colors with dark undertone, uncanny innocence, something is off` | 孩童恐怖、社会恐怖 |
| 10 | 赛博朋克恐怖风 | `cyberpunk horror, neon dystopia, glitch texture, neon glow on dark background, cold metallic tones, digital corruption artifacts` | 科技恐怖、寄生/操控恐怖 |
| 11 | 医学/解剖图鉴风 | `dark medical illustration, anatomical horror, precise line work, clinical detachment, sickly objective tone, vintage anatomy textbook` | 身体恐怖、科技恐怖 |
| 12 | 克苏鲁/宇宙恐怖风 | `cosmic horror illustration, Lovecraftian, deep sea and deep space palette, impossible geometry, human insignificance, vast unknowable entity` | 宇宙恐怖、寄生/操控恐怖 |
| 13 | 剪影/高对比风 | `stark silhouette horror, high contrast, pure black silhouette on monochrome background, minimalist but deeply unsettling, negative space` | 困境恐怖、替身/分身恐怖 |
| 14 | 油画厚涂恐怖风 | `dark oil painting, impasto horror, heavy brush strokes, dark palette, Rembrandt chiaroscuro, thick texture, somber atmosphere` | 谋杀悬疑、社会恐怖 |

选定后填入：**本游戏 CG 风格**：编号 + 风格名称

**CG 风格提示词基底**（由上方选择决定）：
```
{{CG_STYLE_PROMPT_BASE}}
```

> 所有 CG 共享这组风格提示词基底，保证画风统一。开发者根据每张 CG 的具体内容追加主体描述。
> - 医学/解剖图鉴风（dark medical illustration, anatomical horror）——精密线条、解剖式冷静、病态的客观感
> - 克苏鲁/宇宙恐怖风（cosmic horror illustration, Lovecraftian）——深海/深空色调、不可能几何结构、人类渺小感
> - 剪影/高对比风（stark silhouette horror, high contrast）——纯黑剪影+单色背景，极简但极不安
> - 油画厚涂恐怖风（dark oil painting, impasto horror）——厚重笔触、暗色调、伦勃朗式光影

### 配色方案

| 用途 | 方向 | 参考值（不强制） | 说明 |
|------|------|-----------------|------|
| 60% 主色（背景/表面） | 深暗色 | 如 #0a0a0a / #1a0a0a | 近黑，可带微弱红/绿底色 |
| 30% 辅助色（正文文字） | 灰白色 | 如 #b0b0b0 / #c8c0b8 | 不要纯白，偏灰偏暖 |
| 10% 强调色（选择/高亮） | 暗红色 | 如 #8b0000 / #a02020 | 血色，不刺眼 |
| 恐怖强调色 | 病态色 | 如 #4a6741 / #6b4c3b | 用于恐怖文字、异常元素 |
| 安全色 | 暖光色 | 如 #d4a574 | 极少量使用，代表"还没完全完蛋"的微弱希望 |

> 配色原则：整体极暗。画面中 90% 是黑暗，亮色只在文字和极少量强调元素上。CG 插图本身自带色彩，不需要 UI 来补充。

### 排版规范

| 用途 | 字体 | 字号 | 字重 |
|------|------|------|------|
| 章节标题 | 衬线字体（如 serif 系统字体） | 24-32px | 700 |
| 正文叙事 | 无衬线 | 16-18px | 400 |
| 选择文字 | 无衬线 | 15-17px | 400 |
| 恐怖强调文字 | 同正文，用 CSS 特效区分 | 同正文 | 400-700 |

> 排版原则：正文行高 ≥ 1.8 倍字号（比其他类型更宽松，给阅读留呼吸感）。段落间距大一些，让黑暗"渗进"文字之间。

### 间距与圆角

- **间距基准**：8px 网格
- **文字区域内边距**：24-32px（比其他类型更宽松）
- **段落间距**：20-28px（刻意留大，让黑暗填充）
- **圆角**：0-4px（方正、硬朗、不友好）

### 必须实现的视觉反馈

| 用户操作 | 视觉反馈要求 |
|---------|------------|
| 进入新章 | 画面渐暗 → 黑屏 → 章标题缓慢抖动显现 |
| 阅读推进 | 文字逐段渐显（fade in），不是瞬间出现 |
| hover 选择 | 按钮边框变红 + 轻微抖动 + 文字微变形 |
| 做出选择 | 墨水扩散/裂痕效果 → 过渡到下一段 |
| 到达恐怖高潮文字 | 文字可能变色/抖动/重影，UI 边缘渗红 |
| 到达结局 | 画面渐变 + CG 缓慢显现 + 结局文字 |
| 屏幕切换 | 渐暗过渡，不能瞬切 |

### 动效标准

- 所有 UI 过渡时长 300-1000ms（比其他类型更慢，营造压抑节奏）
- 使用 ease-in 或 ease-in-out 缓动（慢启动更有压迫感）
- 背景脉动周期 ≥ 10s，极缓慢
- 恐怖特效（抖动/重影/渗血）持续时间短（200-500ms），不能一直抖
- 尊重用户的 `prefers-reduced-motion`（关闭抖动和脉动，保留渐显和过渡）
- 动效服务于恐怖氛围，不是为了炫技

---

## 五、功能清单

> P0 = 核心体验，P1 = 明显提升体验。只做 P0 和 P1。

| 优先级 | 功能名称 | 解决什么问题                     | 输入 | 输出 |
|--------|----------|----------------------------|------|------|
| P0 | 第一人称叙事 | 玩家需要沉浸在故事中                 | 点击推进 | 逐段显现的恐怖叙事文字 |
| P0 | 选择节点 | 玩家需要参与感和命运感                | 点击选项 | 继续叙事 + 暗中加减分值 |
| P0 | CG 插图展示 | 关键场景需要视觉冲击                 | 到达关键剧情点 | CG 渐显 + 叙事文字 |
| P0 | 章末解密小游戏 | 每章需要游戏性，通关才能进下一章 | 章末触发 | 解密交互（拼图/密码/连线等）|
| P0 | 结局 | 通关后给玩家一个故事结尾 | 最终章完成 | 结局 CG + 结局文字 |
| P0 | 章节过渡 | 恐怖递进需要节奏感                  | 自动触发 | 暗色过渡 + 章标题 |
| P0 | 恐怖 UI 氛围 | 视觉必须强化恐怖感                  | 全程 | 重影/渗血/脉动/抖动等 CSS 效果 |
| P0 | 首启资产准备页 | required 图片必须 ready 后才能进入核心体验 | 打开 app | 检查 IndexedDB；cache miss 时显示生成进度 |
| P0 | AI 生成 CG 插图 | 关键场景必须有画面，必须是调用 AI 用提示词生成的真实图片 | 首次启动缺图时生成 | 每章 1-2 张 CG + 结局 1 张 CG，写入 IndexedDB Blob |
| P0 | AI 生成背景图 | 需要与故事匹配的氛围背景               | 首次启动缺图时生成 | 1 张阴暗氛围背景图，写入 IndexedDB Blob |
| P1 | 存档续玩 | 中途离开后可继续                   | 点击继续 | 从上次进度恢复 |
| P1 | 章节主页 | 玩家需要看到整体进度                 | 进入游戏 / 完成章节后返回 | 章节列表（已解锁/未解锁）+ 进度 |

---

## 六、用户动线

### 核心路径

```
打开 app → 读取 IMAGE_ASSET_MANIFEST → 检查 IndexedDB → cache hit 快速载入 / cache miss 显示资产准备页并生成缺失图片 → required 图片 ready → 标题页 → 章节主页 → 选择章节 → 章节过渡（黑屏+标题）→ [叙事段落 → 选择节点] × 2-3 次 → 章末解密小游戏 → 通关 → 返回章节主页（下一章解锁）→ ... → 最终章解密通关 → 结局 CG + 结局文字
```

### 功能流转关系

| 源功能 | 流向 | 目标功能 | 流转方式 |
|--------|------|---------|---------|
| 打开 app | → | 图片缓存检查 | 读取 `IMAGE_ASSET_MANIFEST` 并查询 IndexedDB |
| 图片缓存检查 | → | 标题页 | cache hit：读取 Blob、load/decode、全部 required 图片 ready |
| 图片缓存检查 | → | 资产准备页 | cache miss：只生成缺失图片，写入 IndexedDB 后再进入标题页 |
| 标题页 | → | 恐怖警告弹窗 | 点击"开始"（首次） |
| 标题页 | → | 章节主页 | 点击"继续"（有存档时跳过警告） |
| 恐怖警告弹窗 | → | 章节主页 | 点击"继续" |
| 恐怖警告弹窗 | → | 标题页 | 点击"离开" |
| 章节主页 | → | 章节过渡 | 点击当前可进入的章节 |
| 章节主页 | → | 已完成章节回看 | 点击已完成章节 |
| 章节过渡 | → | 第一个叙事段落 | 自动过渡 |
| 叙事段落 | → | 下一段叙事 / 选择节点 | 点击继续 |
| 选择节点 | → | 下一段叙事 | 点击选项 |
| CG 触发点 | → | CG 全屏展示 | 自动触发 → 点击继续 |
| 章末叙事 | → | 解密小游戏 | 自动进入 |
| 解密小游戏 | → | 章节主页 | 通关后返回（下一章解锁） |
| 最终章解密通关 | → | 结局页 | 自动跳转 |
| 结局 | → | 章节主页 | 点击"重新开始" |

---

## 七、信息结构

### 导航结构

```
标题页
├── 开始 → 恐怖警告弹窗 → 确认 → 章节主页
└── 继续 → 章节主页（恢复存档，跳过警告）

章节主页（核心导航枢纽）
├── 章节列表
│   ├── 已完成章节 → 可回看（显示完成标记）
│   ├── 当前可进入章节 → 可点击进入
│   └── 未解锁章节 → 锁定
└── 进度展示

章节内（线性推进，不可跳转）
├── 章节过渡（黑屏 + 章标题）
├── 叙事段落 × N
│   ├── 纯叙事（点击推进）
│   ├── 选择节点（2 个选项，纯叙事分支，不计分）
│   └── CG 展示（关键剧情点触发）
└── 章末解密小游戏（通关才能进下一章）

结局页（最终章解密通关后触发）
├── 结局 CG + 结局文字
└── 重新开始 → 章节主页
```

> 线性解锁，必须通过第 N 章才能进入第 N+1 章。已完成章节可回看。

### 游戏界面布局

| 界面 | 核心内容 | 交互方式 |
|------|---------|---------|
| 资产准备页 | required 图片缓存状态、生成进度、当前图片用途、失败重试 | 自动检查 / 重试 |
| 标题页 | 游戏名称（恐怖字体效果）+ 生成的背景图 + 开始/继续按钮 | 点击 |
| 章节主页 | 章节列表（已完成/当前/未解锁）+ 进度 | 点击章节 |
| 叙事界面 | 暗色背景 + 居中文字区域 + 底部点击继续提示 | 点击推进 |
| 选择界面 | 叙事文字 + 2 个选择按钮 | 点击选项 |
| CG 展示 | 全屏 CG 插图 + 底部叙事文字叠加 | 点击继续 |
| 解密小游戏 | 解密界面（类型各异）+ 恐怖氛围保持 | 拖拽/点击/输入 |
| 结局页 | 结局 CG + 结局文字 + 重新开始按钮 | 点击 |

---

## 八、数据设计

### 存储方案

| 维度 | 选择 |
|------|------|
| 存储方式 | localStorage + IndexedDB |
| 多端同步 | 否 |
| 数据上限 | localStorage 只保存轻量 JSON；图片 Blob 使用 IndexedDB |
| 导入导出 | 不支持 |

localStorage 只能保存章节进度、已完成状态、设置、警告确认状态等轻量数据，不得保存图片、base64、大 data URL、Blob 字符串或 object URL。所有 AI 生成背景图、章节 CG 和结局 CG 必须以 Blob 写入 IndexedDB，并通过运行时 `IMAGE_ASSET_RUNTIME_STATE` 判断是否 ready。

### 数据关系

| 实体 A | 关系 | 实体 B | 说明 |
|--------|------|--------|------|
| chapter | 一对多 | narrative_segment | 每章若干叙事段落 |
| chapter | 一对多 | choice_node | 每章 2-3 个选择节点 |
| chapter | 一对少 | cg | 每章 1-2 张 CG |
| chapter | 一对一 | puzzle | 每章末尾 1 个解密小游戏 |
| choice_node | 一对二 | option | 每个选择恰好 2 个选项 |
| ending | 一对一 | cg | 结局 1 张 CG |
| IMAGE_ASSET_MANIFEST | 一对多 | image_asset | 背景图、章节 CG、结局 CG 的静态资产计划 |
| IMAGE_ASSET_RUNTIME_STATE | 一对一 | image_asset | 运行时缓存、加载、解码和 ready 状态 |

---

## 九、交互设计

### 交互模式选择

**本项目选择**：极简阅读 + 选择 + 章末解密

**选择原因**：叙事阶段保持极简沉浸（点击推进 + 选择），章末用解密小游戏提供游戏性和通关门槛。

### 操作映射

| 操作 | 实现方案 | 说明 |
|------|---------|------|
| 推进叙事 | 点击页面任意位置或"继续"按钮 | 下一段文字渐显 |
| 做出选择 | 点击 2 个选项之一 | 纯叙事分支，不计分 |
| 查看 CG | 自动触发 | 到达关键剧情点时 CG 渐显 |
| 解密交互 | 拖拽/点击/输入（取决于解密类型） | 通关后进入下一章 |
| 存档 | 自动 | 每次状态变化写入 localStorage |
| 读档 | 点击标题页"继续" | 进入章节主页，恢复进度 |

### 交互反馈

| 操作 | 视觉反馈 | 状态变化 |
|------|---------|---------|
| 推进叙事 | 新段落文字渐显（fade in） | currentSegment +1 |
| 做出选择 | 选中项墨水扩散效果 → 过渡到下一段 | 无分值变化 |
| CG 触发 | CG 图缓慢渐显（1-2s） | 无 |
| 解密成功 | 通关动画（如拼图合拢、锁打开）+ 过渡 | chapter.completed = true |
| 解密失败 | 温和提示 + 可无限重试 | 无 |
| 解密累计 3 次提交失败 | 出现"查看答案"按钮，点击显示正确答案，关闭后可继续尝试 | 无 |
| 进入新章 | 黑屏 → 章标题抖动显现 | currentChapter +1 |
| 到达结局 | 画面渐暗 → 结局 CG 显现 → 结局文字 | finished = true |

---

## 十、核心机制约束

> 以下是**不可更改的规则**，开发时必须遵守。

### ⚠️⚠️⚠️ CG 插图生成与缓存规则（最高优先级）

> **这是本项目最重要的约束，违反即判定为未完成。**
>
> **CG 插图必须由 AI 生图模型生成，绝对禁止用 CSS / SVG / Canvas / emoji / 文字描述替代。**

`IMAGE_GENERATION_TIMING = first-run-cache`

本产品不设置构建期预置图片例外。标题背景图、每章关键场景 CG、结局 CG 都必须在最终 app 首次启动时按需生成并写入 IndexedDB。用户刷新或下次打开时，如果 `cache_key` 命中且 Blob 可正常 load/decode，只读取 IndexedDB 缓存，不再调用生图工具/API。

只有以下情况才允许重新调用生图：图片缓存缺失、缓存损坏、prompt / 统一画风基底 / 资产清单版本 / spec 版本变化，或用户在设置中明确触发重新生成。除此之外，后续进入产品必须复用 IndexedDB 中已经缓存的真实图片 Blob。

**什么是"生图"：**
- ✅ 调用生图模型（如图像生成 API / 工具），输入提示词，获得一张真实的图像文件
- ✅ 本 spec 中的图片由最终 app 首次启动时生成后写入本机 IndexedDB 缓存
- ✅ 用户正式进入依赖图片的体验时，页面引用的必须是真实生成图片 Blob 生成的 object URL 或等效缓存读取结果
- ❌ 用 CSS 渐变、box-shadow、border-radius 画出来的图形 — **这不是生图**
- ❌ 用 SVG path 手动描绘的图形 — **这不是生图**
- ❌ 用 Canvas 代码绘制的图形 — **这不是生图**
- ❌ 用 emoji 或 Unicode 符号拼凑 — **这不是生图**
- ❌ 用纯文字描述"这里应该有一张图" — **这不是生图**

**first-run-cache 固定流程：**

```text
打开 app
→ 读取 IMAGE_ASSET_MANIFEST
→ 为每张 required 图片计算 / 校验 cache_key
→ 查询 IndexedDB
→ cache hit：读取 Blob → 创建临时 object URL → load/decode → IMAGE_ASSET_RUNTIME_STATE.ready = true
→ cache miss：显示资产准备页 → generation lock → 调用真实生图工具/API → Blob 写入 IndexedDB → load/decode → ready = true
→ 所有 required 图片 ready
→ 进入标题页和核心体验
```

资产准备页必须主题化，显示已完成数量 / 总数量、当前图片用途、失败原因和重试入口，不能是空白 loading 页。cache hit 时只能显示短暂"正在载入图片 / 正在打开故事"的轻量状态，不显示完整生成进度，不误导用户以为正在重新生图。

**生图提示词要求：**

1. **为每张 required 图片编写详细的生图提示词**：
   - 以 CG 风格提示词基底为前缀
   - 追加该场景的具体描述（场景、氛围、关键元素、光影）
   - 提示词必须足够详细，至少 30 词
2. **所有图片共享统一画风基底**：
   - `STYLE_PROMPT_BASE` 由恐怖子类型、核心场景、媒介、光影、材质和构图共同决定
   - 完整 prompt = `STYLE_PROMPT_BASE` + 单张图片的主体、场景、动作、情绪、光影、用途说明
3. **提示词必须进入 `IMAGE_ASSET_MANIFEST`**：
   - 不得只在开发过程临时保存
   - 每张图必须有 `prompt_hash` 和稳定 `cache_key`

**IMAGE_ASSET_MANIFEST 要求：**

开发者在写核心页面前必须先输出并核对 `IMAGE_ASSET_MANIFEST`。每张 required 图片至少包含：

| 字段 | 说明 |
|------|------|
| `id` | 图片唯一 ID，如 `title_background` / `chapter_01_cg_01` / `ending_cg` |
| `purpose` | `title_background` / `chapter_cg` / `ending_cg` |
| `required` | 是否为进入核心体验前必须完成；标题背景、章节 CG、结局 CG 默认为 required |
| `plan_status` | 固定为 `required` / `optional`，不得表示已生成 |
| `initial_runtime_status` | `pending` / `seed_available` |
| `style_prompt_base` | 本项目统一画风基底 |
| `prompt` | 该图片完整正向提示词 |
| `negative_prompt` | 项目级短模板或简洁排除项 |
| `aspect_ratio` | 目标比例，如 16:9 / 4:3 |
| `generation_timing` | 固定为 `first-run-cache` |
| `cache_key` | 稳定缓存键，建议由 `game_id + asset_manifest_version + asset_id + prompt_hash` 组成 |
| `prompt_hash` | prompt + style prompt base + negative prompt + asset manifest version 的哈希 |
| `seed_source` | 默认 `{ "type": "none" }` |
| `storage_driver` | 固定为 `indexeddb_blob` |

静态 `IMAGE_ASSET_MANIFEST` 只描述资产计划，不表示当前浏览器里图片已经可用。不得在静态计划里用 `status = generated` / `cached` 放行体验；`placeholder`、`css_fallback`、`svg_fallback` 均不通过验收。

**IMAGE_ASSET_RUNTIME_STATE 要求：**

运行时必须为每张 required 图片维护一条状态记录：

| 字段 | 说明 |
|------|------|
| `asset_id` | 对应 `IMAGE_ASSET_MANIFEST.assets[].id` |
| `cache_key` | 与静态计划一致 |
| `generation_status` | `pending` / `generating` / `generated` / `failed` |
| `cache_status` | `not_checked` / `cache_hit` / `cache_miss` / `cached` / `cache_corrupt` |
| `cached_blob_ref` | IndexedDB 中的 Blob 引用信息，不存 object URL |
| `loaded` | 是否已被浏览器加载 |
| `decoded` | 是否已完成解码 |
| `ready` | 生成状态、缓存状态、加载、解码和安全状态均通过后才为 `true` |
| `error` | 失败原因，成功时为空 |

进入标题页和核心体验前，每个 required 图片都必须有 `IMAGE_ASSET_RUNTIME_STATE.ready = true`。

**IndexedDB 缓存要求：**

- 图片 Blob 必须优先存入 IndexedDB。
- localStorage 只保存少量 metadata、版本号、用户进度和完成状态，不存图片、base64、大 data URL、Blob 字符串或 object URL。
- 页面显示时可以用 `URL.createObjectURL(blob)` 创建临时 object URL，但 object URL 只能用于当前会话，不能当作长期存储。
- cache hit 时不得调用生图工具/API；只能读取 IndexedDB Blob、创建临时 object URL、load/decode，并更新运行时状态。
- cache miss、缓存损坏或版本变化时，只生成缺失 / 失效图片，生成后必须写入 IndexedDB，再执行 load/decode。
- 必须使用生成锁，避免刷新、双击或多个标签页同时触发同一图片重复生图；优先使用 `navigator.locks`，不支持时用 IndexedDB 中的 `generation_lock` 记录实现。
- required 图片多次生成失败时，停留在资产准备页或错误页，说明失败资产 ID、用途、失败原因和已尝试 prompt。

**CG 数量要求**：
- 每章至少 1 张场景 CG（关键恐怖场景）
- 结局 1 张 CG
- 背景图 1 张
- 总计：章数 + 1（结局）+ 1（背景）张，全部必须是 AI 生成的真实图片

**如果无法生图怎么办**：
- **不可以**静默地用 CSS 画一个"替代品"然后当作完成了
- **必须**停下来，明确告诉用户："我需要生图能力来生成 CG 插图，但当前没有可用的生图工具。请提供生图工具/API，或者告诉我如何调用。"
- 在生图能力可用之前，不得交付声称完成的最终成品；required 图片不得被标记为 ready

### 叙事规则

**第一人称视角**：
- 全程用"你"作为主语——"你推开门"、"你听到身后有声音"
- 叙事语气从平静逐章变为紧张、慌乱、绝望
- 每段叙事 60-150 字，不要太长——恐怖叙事需要节奏感，短段落 + 留白 = 压迫感

**恐怖递进**：
- 第 1 章：几乎正常，只有 1-2 个极微妙的不对劲（玩家可能注意不到）
- 第 2-3 章：异常越来越明显，但还有合理解释
- 第 4-5 章：细节积累，不安加深，玩家开始主动怀疑
- 倒数第 2 章：合理解释崩塌，恐怖浮出水面
- 最终章：高潮 + 结局

**叙事深度要求**：
- 6-8 章的篇幅意味着故事必须有足够的层次和细节，不能三章讲完核心然后注水
- 每章应有独立的小悬念或揭示，而不只是"继续往前走"
- 人物（如果有）需要有性格、动机、前后变化
- 环境描写要具体、感官化——不是"你感到害怕"，而是描述让人害怕的具体细节
- SKILL 中的知识内容应自然融入叙事，成为故事的血肉而非装饰

**CG 触发时机**：
- CG 应出现在每章最具视觉冲击力的时刻
- CG 显现前有一段铺垫文字，CG 显现后有一段反应文字
- CG 不要太频繁——物以稀为贵，一章 1 张就够

### 选择节点规则

- 每个选择节点恰好 2 个选项
- 两个选项推向不同的下一段叙事（短暂分叉后汇合回主线）
- 选择不计分，不影响结局，纯粹丰富叙事体验
- 选项文字本身要有恐怖叙事感，不能是干巴巴的"A/B"
- 不同选择可以让玩家看到不同的细节/视角，增加重玩价值

### 章末解密小游戏规则

> **每章末尾必须有 1 个解密小游戏，通关才能解锁下一章。** 这是本游戏的核心游戏性来源。

**解密类型库**（开发者为每章选择不同类型，避免重复）：

| 类型 | 玩法 | 实现方式 | 恐怖适配建议 |
|------|------|---------|------------|
| 拼图 | 拖拽碎片拼出完整图片 | CSS grid + 拖拽 | 拼的过程中图片逐渐成形，内容越来越不对 |
| 密码锁 | 输入正确的数字/文字组合 | 输入框 + 验证 | 线索藏在前面的叙事里，像是在撬开不该打开的东西 |
| 滑块解锁 | 滑动多个滑块到正确位置 | range input + SVG | 像是在调频，调到正确频率时听到/看到恐怖内容 |
| 连线匹配 | 把左侧元素和右侧元素正确连线 | SVG line + 拖拽 | 连出来的结果揭示一个恐怖的真相 |
| 翻牌配对 | 记忆翻牌找到配对 | CSS flip + JS | 翻到的图案越来越诡异 |
| 序列还原 | 把打乱的事件/图片排回正确顺序 | 拖拽排序 | 排出来的时间线揭示恐怖真相 |
| 寻找异常 | 在画面中找出不对的地方 | SVG/CSS 画面 + 点击热区 | "这张图有什么不对？"细思极恐 |
| 迷宫 | 操控角色走出迷宫 | Canvas/SVG 绘制 | 在黑暗中摸索，路线暗示某种图案 |

**解密设计约束**：
- 每章必须选择不同的解密类型，**同一个游戏中不可重复使用同一类型**
- 解密的视觉风格必须与恐怖氛围一致（暗色、不安、诡异）
- 解密失败可无限重试，不惩罚
- 累计 3 次提交/确认失败后，出现"查看答案"按钮；点击后显示正确答案，关闭后可继续尝试
- 解密难度适中——不能太简单（30 秒完成没意思），不能太难（卡 10 分钟打断叙事节奏）
- 解密用 CSS / SVG / Canvas / JS 实现（这是交互游戏，不是 CG，可以用代码画）

**每章解密设计表**（填写 spec 时必填）：

| 章 | 解密类型 | 剧情嵌入 | 涉及元素 | 通关条件 |
|----|---------|---------|---------|---------|
| {{CH_NUM}} | {{PUZZLE_TYPE}} | {{NARRATIVE_CONTEXT}}（这个解密在故事里是什么） | {{ELEMENTS}}（几块拼图/几个数字/几条线索） | {{WIN_CONDITION}} |

### 章节主页规则

- 线性解锁：通过第 N 章才能进入第 N+1 章
- 已完成章节可回看（重看叙事和 CG，不重新解密）
- 当前可进入的章节高亮显示
- 未解锁章节灰色锁定
- 进度展示：如 3/6 章

### 结局页

- 最终章解密通关后触发
- 结局 CG 缓慢渐显（2-3s）
- 结局文字在 CG 下方或叠加在 CG 上
- 1 个固定结局
- "重新开始"按钮——重新开始会清除进度

### 数据 Schema

#### meta.json

```json
{
  "game_title": "游戏标题",
  "game_subtitle": "一句话定位",
  "horror_subtype": "恐怖子类型",
  "cg_style_prompt_base": "CG 风格提示词基底",
  "closing_message": "结局结语"
}
```

#### chapters.json

```json
{
  "chapters": [
    {
      "id": "ch1",
      "title": "章节标题",
      "atmosphere": "正常/微妙不安/明显异常/恐怖高潮/结局",
      "segments": [
        {
          "id": "ch1_s1",
          "type": "narrative",
          "text": "叙事文字（60-150 字）",
          "has_cg": false
        },
        {
          "id": "ch1_s2",
          "type": "choice",
          "text": "选择前的叙事铺垫",
          "options": [
            { "id": "a", "text": "选项文字", "follow_text": "选择后的叙事衔接" },
            { "id": "b", "text": "选项文字", "follow_text": "选择后的叙事衔接" }
          ]
        },
        {
          "id": "ch1_s3",
          "type": "narrative",
          "text": "叙事文字",
          "has_cg": true,
          "cg": {
            "image_asset_id": "chapter_01_cg_01",
            "prompt": "完整的生图提示词（风格基底 + 场景描述）",
            "alt": "CG 内容描述（无障碍）"
          }
        }
      ],
      "puzzle": {
        "type": "拼图/密码锁/滑块解锁/连线匹配/翻牌配对/序列还原/寻找异常/迷宫",
        "narrative_context": "这个解密在故事里是什么（如：拼合一张被撕碎的照片）",
        "elements": "具体元素描述（如：6 块拼图碎片）",
        "win_condition": "通关条件（如：所有碎片归位）",
        "difficulty_note": "难度说明（如：中等，约 2-3 分钟）"
      }
    }
  ]
}
```

#### ending.json

```json
{
  "ending": {
    "title": "结局标题",
    "text": "结局文字（100-200 字）",
    "cg": {
      "image_asset_id": "ending_cg",
      "prompt": "结局 CG 的完整生图提示词",
      "alt": "CG 内容描述"
    }
  }
}
```

#### image_assets.json

```json
{
  "asset_manifest_version": "v1",
  "image_generation_timing": "first-run-cache",
  "assets": [
    {
      "id": "title_background",
      "purpose": "title_background",
      "required": true,
      "plan_status": "required",
      "initial_runtime_status": "pending",
      "style_prompt_base": "统一恐怖 CG 画风基底",
      "prompt": "标题背景图完整提示词",
      "negative_prompt": "text, logo, watermark, low quality",
      "aspect_ratio": "16:9",
      "generation_timing": "first-run-cache",
      "cache_key": "{{GAME_ID}}:v1:title_background:{{PROMPT_HASH}}",
      "prompt_hash": "{{PROMPT_HASH}}",
      "seed_source": { "type": "none" },
      "storage_driver": "indexeddb_blob"
    }
  ]
}
```

> **JSON 注意事项**：字符串值中需要引号时，必须用中文引号「」或''，绝对不能用英文双引号 `"`，否则会破坏 JSON 结构。

---

## 十一、章节与内容概览

### 章节概览

| 章 | 标题 | 氛围梯度 | 选择节点数 | CG 数 | 解密类型 | 关键恐怖点 |
|----|------|---------|-----------|-------|---------|-----------|
| {{CH_NUM}} | {{CH_TITLE}} | {{ATMOSPHERE}} | {{CHOICES}} | {{CG_COUNT}} | {{PUZZLE_TYPE}} | {{HORROR_POINT}} |

### 恐怖递进曲线

```
恐怖感
  ▲
  │           ╱\
  │          ╱  \      ╱\
  │    ╱\   ╱    \    ╱  ╲ ← 结局
  │   ╱  ╲ ╱      \  ╱
  │  ╱    ╳        ╲╱
  │ ╱
  │╱ ← 几乎正常
  └──────────────────────→ 章节
   1     2     3     4     5
```

> 恐怖感整体递增，但每章内部有起伏（紧张→缓一口气→更紧张），不是直线上升。

### CG 清单

| CG 编号 | 所属章节/结局 | 场景描述 | 提示词要点 |
|---------|-------------|---------|-----------|
| {{CG_NUM}} | {{LOCATION}} | {{SCENE}} | {{PROMPT_KEY}} |

### 每章内容规模

- 每章 8-15 个叙事段落，每段 60-150 字
- 每章 2-3 个选择节点
- 每章 1-2 张 CG
- 每章末尾 1 个解密小游戏（类型各不同）
- 结局 1 张 CG
- 全游戏 1 个固定结局

---

## 十二、技术约束

| 维度 | 选择 |
|------|------|
| 技术栈 | 单页 HTML + CSS + JS + IndexedDB 图片缓存 + 安全可用的运行时生图能力 |
| 部署方式 | 单页产品；首次缺图时需要可调用真实生图工具/API |
| 离线可用 | 首次缺图时不可离线；同一浏览器 / 同一域名完成缓存后可读取 IndexedDB 复用 |
| 浏览器兼容 | 仅现代浏览器 |
| 外部依赖 | 零依赖（字体可用 CDN，有 fallback） |
| 性能要求 | cache hit 首屏快速进入；cache miss 显示资产准备页和生成进度；CSS 动效流畅 |
| CG 存储 | IndexedDB Blob（**必须是 AI 生成的真实图片，不是 CSS 绘制**） |

---

## 十三、开发指引

```
收到本 spec.md 后：
1. 进入 plan 模式，分析 SKILL 文件内容
2. 根据素材内容选择恐怖子类型
3. 设计完整的故事线（5-7 章，第一人称，恐怖递进）
4. 设计每章的选择节点（纯叙事，不计分）
5. 为每章设计不同类型的解密小游戏（类型不可重复）
6. 设计 1 个固定结局
7. 确定 CG 风格提示词基底，并生成 `IMAGE_ASSET_MANIFEST`
8. 为标题背景图、每章 CG、结局 CG 编写详细 prompt、negative prompt、prompt_hash 和 cache_key
9. 实现首次启动资产准备页、IndexedDB Blob 缓存、生成锁、失败重试和 ready 闸门：
   - cache hit 只读取 IndexedDB Blob、load/decode，不调用生图
   - cache miss 才调用真实生图工具/API，生成后写入 IndexedDB
   - 如果没有可用的生图能力，必须停下来告知用户，不可以用 CSS 替代
10. 实现 UI 框架（阴森风格 + 恐怖 CSS 特效）
11. 实现叙事系统 + 选择系统
12. 逐章实现解密小游戏（CSS/SVG/Canvas/JS 实现）
13. 实现章节主页 + 结局页
14. 写入 localStorage 进度，不把图片、Blob、base64 或 object URL 存进 localStorage
15. 遵守「反套路约束」和「核心机制约束」中的所有规则
16. 完成后执行全量自检（对照验收标准）
```

---

## 十四、验收标准

### 功能验收

| 功能 | 验收条件 | 自测结果 |
|------|---------|---------|
| 标题页 | 游戏名称 + 恐怖氛围 + 开始/继续按钮 | [ ] 通过 |
| 恐怖警告弹窗 | 点击开始后弹出，恐怖风格 UI（扭曲字体/腐蚀边缘/抖动），继续/离开按钮 | [ ] 通过 |
| 章节主页 | 章节列表（已完成/当前/未解锁）+ 进度 | [ ] 通过 |
| 叙事系统 | 第一人称文字逐段渐显 | [ ] 通过 |
| 选择节点 | 2 个选项，选后继续叙事 | [ ] 通过 |
| CG 展示 | 关键剧情点显示 AI 生成的 CG 插图 | [ ] 通过 |
| 章末解密 | 每章末尾有解密小游戏，类型各不同，通关才能进下一章 | [ ] 通过 |
| 结局 | 最终章通关后展示结局 CG + 结局文字 | [ ] 通过 |
| 章节过渡 | 黑屏 + 章标题抖动显现，不瞬切 | [ ] 通过 |
| 恐怖 UI 特效 | 重影/渗血/抖动等 CSS 效果正常 | [ ] 通过 |
| 存档续玩 | 刷新后进度保留 | [ ] 通过 |

### ⚠️ CG 插图验收（最高优先级）

> **以下每一条都必须通过，否则判定为未完成。**

- [ ] 每章至少 1 张 CG — 是 AI 生图模型生成的真实图片，不是 CSS/SVG/Canvas 绘制
- [ ] 每个结局有 1 张 CG — 是 AI 生图模型生成的真实图片
- [ ] 背景图 — 是 AI 生图模型生成的真实图片
- [ ] 所有 CG 风格统一（使用同一套提示词基底）
- [ ] `IMAGE_GENERATION_TIMING` 明确为 `first-run-cache`
- [ ] 存在静态 `IMAGE_ASSET_MANIFEST`，每张图包含 `cache_key`、`prompt_hash`、`seed_source` 和 `storage_driver = indexeddb_blob`
- [ ] 存在运行时 `IMAGE_ASSET_RUNTIME_STATE`，required 图片在用户进入核心体验前全部 `ready = true`
- [ ] 图片生成后以 Blob 写入 IndexedDB；页面引用真实位图 Blob 生成的 object URL 或等效缓存读取结果
- [ ] cache hit 时只读取 IndexedDB 缓存，不调用生图工具/API
- [ ] cache miss 时只生成缺失图片，并在写入 IndexedDB 后执行 load/decode
- [ ] 存在生成锁，刷新、双击或多标签页不得并发重复调用生图
- [ ] 没有用 CSS 渐变/阴影/滤镜"画"出来的假图冒充 CG
- [ ] 没有用 SVG path 描绘的图形冒充 CG
- [ ] 没有用 emoji/Unicode 符号拼凑冒充 CG
- [ ] 没有留空或写"待生图"的占位符被标记为完成

### 叙事验收

- [ ] 全程第一人称（"你"视角）
- [ ] 恐怖感逐章递进，第 1 章接近正常
- [ ] 没有使用禁止的套路元素
- [ ] 恐怖感来自理解而非惊吓
- [ ] 至少有 1 个"回头看才发现不对"的设计
- [ ] 每段叙事 60-150 字，节奏感好
- [ ] 故事有足够的深度和层次（6-8 章不注水，每章有独立悬念）
- [ ] 环境和细节描写具体、感官化，不是泛泛的"你感到害怕"
- [ ] 故事与素材内容有内在联系

### 选择与解密验收

- [ ] 每个选择恰好 2 个选项
- [ ] 选择不计分，纯叙事分支
- [ ] 每章末尾有 1 个解密小游戏
- [ ] 各章解密类型互不重复
- [ ] 解密可无限重试，累计 3 次失败后出现"查看答案"按钮
- [ ] 解密通关后才能进入下一章
- [ ] 解密视觉风格与恐怖氛围一致
- [ ] 结局有 CG 和文字
- [ ] 重新开始会清除进度

### 数据验证

- [ ] 所有 ID 引用一致
- [ ] 游戏状态刷新后保留（localStorage）
- [ ] localStorage 只保存轻量进度、设置和完成状态
- [ ] localStorage 不保存图片、base64、大 data URL、Blob 字符串或 object URL
- [ ] IndexedDB 图片缓存记录包含 asset_id、asset_manifest_version、cache_key、prompt_hash、blob、mime_type、status、created_at、updated_at、error
- [ ] JSON 语法合法，字符串内引号用中文引号「」

### 交互验证

- [ ] 点击推进叙事正常
- [ ] 选择按钮有 hover 恐怖反馈（变红/抖动）
- [ ] 屏幕切换有暗色过渡，不瞬切
- [ ] CG 显示有渐显动画

### 视觉验收

- [ ] 背景图已由 AI 生图模型生成，写入 IndexedDB，并通过 Blob/object URL 显示
- [ ] 背景图上方有半透明暗色遮罩
- [ ] 整体色调极暗，符合恐怖氛围
- [ ] 恐怖 CSS 特效正常（重影/渗血/脉动/抖动）
- [ ] 文字可读性良好（暗底 + 灰白字）
- [ ] 章节过渡有压抑的仪式感
- [ ] 排版间距宽松，黑暗填充文字间
- [ ] 已实现 `prefers-reduced-motion` 适配

### 响应式验证

- [ ] 桌面端（≥1024px）布局正常
- [ ] 平板端（768px）布局自适应
- [ ] 手机端（≤480px）核心功能可用

---
