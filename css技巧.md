# CSS 技巧

这份文件单独整理高质量 CSS 的通用技巧。它不接入 `specforge` 流程，只作为写 spec 或实现界面时的参考笔记，是 `svg技巧.md` 的对等篇。

核心原则：CSS 不是给元素逐个"刷样式"，而是一套由**主题驱动、有层级、有状态、有节奏、可复用、可降级**的视觉系统。绝大多数难看的界面，不是缺某个效果，而是没有系统：颜色、间距、阴影、动效各编一套，凑在一起就乱。

## 1. 主题驱动，不逐元素刷样式

- 先定**这个产品该是什么感觉**（媒介、情绪、材质、光影、时代），再决定颜色、形状、动效，而不是反过来从控件默认样式拼凑。
- 同一套视觉语言必须**贯穿全程**：首页、内部、结算页质感一致，用户不应觉得"进了另一个网页"。
- 任何一个具体样式值出现前，先问它属于哪个系统（色板？间距？层次？状态？）。落单的魔法值是系统破洞的信号。
- 禁止浏览器默认样式裸奔：按钮、表单、滚动条、focus 圈都要纳入主题，否则它们会出卖整套设计。

## 2. 设计 token 与 CSS 变量

把视觉决策集中成 token（`:root` 自定义属性），而不是散落在各处。

| token 类别 | 承载什么 |
|------------|----------|
| 语义色 | `--bg`、`--surface`、`--accent`、`--text`、`--success`、`--danger`，按角色命名而非按色相 |
| 间距 | 一套有节奏的间距阶梯（如 4 的倍数），不要随手 `13px` |
| 圆角 / 边框 | `--radius`、`--border`，统一形状语言 |
| 层次 / 阴影 | `--elevation-1/2/3`，统一深度语言 |
| 动效 | `--duration-*`、`--ease-*`，统一节奏 |
| 状态 | `--focus-ring`、`--state-hover`、`--state-disabled` |

要点：

- **按语义命名，不按外观**：`--accent` 而不是 `--orange`，换主题只改值不改用处。
- 主题切换（深浅、风格）= 换一组 token，不是改一堆选择器。
- 数值有了名字才有系统；同一个值在三处出现，就该是一个 token。

## 3. 信息层级：靠间距、字号、对齐，不靠装饰

- 层级优先用**留白、字号阶梯、字重、对齐**建立，其次才是颜色，最后才是边框/卡片/装饰。
- 舍得留白。挤满的界面没有重点；一屏要有明确的主次和呼吸。
- 排版本身就是设计：字号阶梯、行距、字距、对齐线，是秩序的来源。标题不是放大的正文。
- 数值、读数用稳定宽度容器（等宽或定宽），避免内容变化时布局跳动。

## 4. 层次、光影与材质：一致的"深度语言"

- 选**一个光源方向**和**一套高度阶梯**，全局一致地表达深度。阴影是"高度 + 光"的结果，不是随手加的装饰。
- 阴影、高光、边框、模糊是表达"材质和层次"的语言：玻璃、纸、金属、霓虹各有质感，但同一产品内规则要统一，别每个组件各编一套。
- 用层次而不是描边来分隔：优先用背景色阶、阴影、间距区分区域，`1px solid` 切一切是廉价感的来源。
- 材质服从主题：通透玻璃、温暖纸感、冷硬科技，是从产品气质推出来的，不是套效果。

## 5. 状态是一等公民

每个可交互元素都要**预设完整状态集**，不是事后补。

| 状态 | 原则 |
|------|------|
| default / hover / active | 可感知的即时反馈，按下要有"被按到"的回应 |
| focus-visible | 键盘可达，焦点清晰，不要为了好看删掉 focus 圈 |
| selected | 选中态明确，但不破坏元素原本语义 |
| disabled | 明确不可用，不误导，不只是变浅 |
| loading / empty / error / success | 这些"非理想态"也要设计，不能是白屏或裸文字 |

要点：

- 反馈用**至少两个通道**（颜色 + 位移/缩放/阴影/描边），不能只靠变颜色——只变色既弱又不可访问。
- 状态变化要可逆、可读：反馈短暂出现后回到基准态，不永久改写基准语义色。

## 6. 动效服务理解与反馈

- 动效是用来表达**因果、连续、层级、反馈**的，不是装饰。问每个动画"它让用户理解了什么"。
- 一切缓动，禁止匀速线性；切换淡入淡出或位移过渡，**不瞬切**。瞬切让用户看不出前后关系。
- 区分**核心动效**（承载机制/反馈，如状态切换、进度、因果演示）和**装饰动效**（背景漂浮、光晕脉动）。
- `prefers-reduced-motion` 下：保留核心动效的低频版本，关闭装饰循环——不是把动画全删，那会让机制不可理解。
- 优先动 `transform` 和 `opacity`（见 §10），它们便宜且流畅。

## 7. 氛围与粒子分层

- 氛围是**背景层**，烘托主题，永远不和核心内容抢注意力，也不拦交互（`pointer-events: none`）。
- 用图层表达深度：背景 < 氛围/粒子 < 内容 < 浮层，各司其职。
- 粒子要有**数量与性能纪律**（通常 10–30，大量改用 Canvas），并在低动效模式下关闭。
- 氛围从主题推导（夜→星尘、火→灰烬、水→气泡……），是产品气质的延伸，不是随机噪点。

## 8. 响应式与适配

- 默认流式：用相对单位、`clamp()`、`min/max`、Grid/Flex 自适应，断点用来**重排结构**，不是微调外观。
- 移动端可以折叠面板、收起次要信息，但**核心内容和操作不能被压扁或裁掉**。
- 触控目标够大（建议 ≥ 40px），hover 专属反馈要有触屏替代。
- 同一套 token 适配多端，不为每个尺寸重写一套样式。

## 9. CSS / SVG / Canvas 的边界

| 技术 | 适合负责 |
|------|----------|
| CSS / HTML | 布局、UI、控件、文字、状态反馈、面板、过渡、简单光影与渐变 |
| SVG | 精确矢量结构、图标、可点击对象、图例、坐标轴（详见 `svg技巧.md`） |
| Canvas | 大量粒子、连续流体/场、实时模拟 |

- CSS 负责"界面与状态"，别用 CSS 硬画复杂矢量结构，也别用上百 DOM 节点模拟粒子。
- 三者共享同一套 token 和状态数据，视觉才统一。

## 10. 性能与合成

- 动画优先 `transform` / `opacity`（走合成层，不触发重排重绘）；避免动画 `width/height/top/left/margin` 这类引发 layout 的属性。
- 慎用高开销效果：大面积 `filter: blur`、`backdrop-filter`、多层阴影叠加会拖帧，按需使用并控制范围。
- 避免布局抖动：批量读写、用 `contain` / `will-change`（克制使用）隔离重绘区域。
- 高频更新只改受影响的元素，别整树重排；不在 `localStorage` 存大段样式/状态字符串。

## 11. 可访问性与优雅降级

- 文本与背景**对比度要够**，强调色不能牺牲可读性；不要用纯颜色传递唯一信息。
- 尊重系统偏好：`prefers-reduced-motion`、`prefers-color-scheme`、可达的 `:focus-visible`、可键盘操作。
- 高级效果要有兜底：`backdrop-filter`、`color-mix`、新特性不被支持时，退化成不透明背景/纯色也能用，而不是塌掉。
- 深浅主题不是简单反色：语义色、对比、层次都要各自成立。

## 12. 组织与可维护性

- 按职责组织样式（token / 基础 / 布局 / 组件 / 状态 / 主题），而不是一坨全局选择器。
- 控制选择器权重和嵌套深度，避免 `!important` 打架；状态用类（`.is-active`）表达，不靠内联样式堆。
- 复用优先：重复出现的样式抽成类或变量；同一效果别在三个地方各写一遍。
- 命名表达意图（`.card--locked`、`.btn--primary`），不要 `.div1`、`.style2`。

## 13. 常见错误

- 浏览器默认样式裸奔：按钮、表单、滚动条、focus 圈没主题化。
- 反馈只变颜色：弱、不可访问，缺少位移/缩放/阴影等第二通道。
- 魔法值满天飞：颜色、间距、圆角各写各的，没有 token，改一处要翻全文件。
- 阴影/光影不一致：每个组件各编一套光源和深度，整体显廉价。
- 用 `1px solid` 切一切：缺少用层次和留白分隔的能力。
- 动画引发重排：动 `width/left/margin` 而不是 `transform`，卡顿。
- 粒子拦截点击或拖垮性能：没 `pointer-events: none`，或用海量 DOM 节点。
- `prefers-reduced-motion` 下把动效全删：核心机制变得不可理解。
- 装饰压过层级：效果太多，重点反而消失。
- 一个断点压扁内容：移动端把核心信息挤变形或裁掉。

## 14. 快速清单

```text
先定主题气质，再决定颜色/形状/动效；不逐元素刷样式。
颜色、间距、圆角、阴影、动效、状态全部 token 化，按语义命名。
层级靠间距/字号/对齐建立，装饰最后上。
选一个光源、一套高度阶梯，全局一致表达深度。
每个可交互元素预设完整状态集，反馈至少两个通道。
动效表达因果与反馈，缓动不瞬切；reduced-motion 留核心关装饰。
氛围/粒子是背景层，不抢内容、不拦交互、可降级。
默认流式响应，断点重排结构，核心内容不被压扁。
CSS 管界面与状态，SVG 管矢量结构，Canvas 管大量动态，三者共享 token。
动画走 transform/opacity；慎用大范围 blur/backdrop-filter。
高级特性有兜底，尊重对比度和系统偏好。
```
